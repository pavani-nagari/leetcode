select e1.employee_id, e1.name, count(e2.employee_id) as reports_count, ROUND(AVG(e2.age)) as average_age from Employees e1 INNER JOIN Employees e2 ON e1.employee_id = e2.reports_to group by e1.employee_id, e1.name order by 1 asc;